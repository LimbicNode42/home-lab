# Format: ENV_NAME|folder|item|field
# Runtime-only FMP provider key for supervised ASX screener smoke/top-N runs.
# Secret values stay in Vaultwarden/BW and are injected only into the child command
# by scripts/secrets/run-with-vaultwarden-env.sh.
#
# Current actual item verified in Vaultwarden; folder names are case-sensitive in bw:
FMP_API_KEY|Homelab|FMP_API_KEY|password
# EODHD fundamentals feed (multi-market; folder Homelab, LOGIN item -> login.password):
EODHD_API_KEY|Homelab|EODHD_API_KEY|password
#
# Preferred normalized future item after the Vaultwarden item is migrated/created:
# FMP_API_KEY|Homelab|investment-screener/fmp|api_key
