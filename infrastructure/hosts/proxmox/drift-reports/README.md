# Proxmox drift reports

Store dated read-only comparisons between live Proxmox state and desired-state seeds here.

Do not store API tokens, task logs containing secrets, or full unredacted guest configs with credentials. Prefer sanitized JSON summaries under `inventory/discovery/` plus human-readable drift notes here.
